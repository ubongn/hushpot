{
  "name": "midnight-run",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Hushpot - M1 Compact contract with deliberate disclosure boundaries (Midnight hackathon)",
  "scripts": {
    "compile": "node scripts/compile.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "@midnight-ntwrk/compact-runtime": "0.16.0"
  }
}
